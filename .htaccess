<IfModule mod_rewrite.c>
    RewriteEngine On

    # Skip if request already targets /public
    RewriteRule ^public/ - [L]

    # Send all other requests to the public directory
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>